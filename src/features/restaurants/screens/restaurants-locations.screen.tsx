import React from "react";
import { Text } from "react-native";
import { SafeArea } from "../../../componentes/utils/safe-area.component";

const RestaurantsLocationsScreen: React.FC = () => {
  return (
    <SafeArea>
      <Text>Locations</Text>
    </SafeArea>
  );
};

export default RestaurantsLocationsScreen;
